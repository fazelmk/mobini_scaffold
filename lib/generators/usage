Description:
    scaffold, migration and nested_form

SCAFFOLD:
  Modo de uso do mks:
    rails generate mks model 'attribute:type{options}:index' 'attribute:type{options}:uniq' ...   
    exp.: rails g mks Test 'name:string{limit:100,default:"test"}' has_test:boolean{default:true} test_index:integer{default:10}:index test_uniq:integer:uniq

  Options
     --namespace=namespacename ```model with namespace```
     --replace_shared ```Replace gem files generated in project```
     --without-export ```generate index without the export buttons```
     --without-import ```generate index without the import button```

  Note:
    If the attribute got 2 options or if it has a string. It must be between single quotes >> ' <<

MIGRATION
  Modo de uso do mkm:
    rails generate mkm AddXxxToYyy 'attribute:type{options}:index' 'attribute:type{options}:uniq' ...   
    exp.: rails g mks AddVisibleToTest visible:boolean

  Options
     --namespace=namespacename ```when adding stuff to model that has namespace```

  Note:
    If the attribute got 2 options or if it has a string. It must be between single quotes >> ' <<
    If the model is in a namespace, the namespace has to go in the opcion "namespace"


NESTED_FORM_GENERATOR
  Modo de uso do mknf:
    rails generate mknf model parent_model 'attribute:type{options}:index' 'attribute:type{options}:uniq' ...   
    exp.: rails g mknf NestedChild ParentModel name:string visible:boolean

  Options
     --namespace=namespacename ```namespace of the model```

  Note:
    If the attribute got 2 options or if it has a string. It must be between single quotes >> ' <<
    If the parent model has namespace use it as normaly done. Ex.: namespace/model:references


Custom Types
  price
  file
  image
  enum (usage 'field:enum{enum:["value1";"value2";"value3"]}')
  wday (for enum of weekdays)
  phone
  custom reference (Ex.: 'my_model_tag:references{class_name:namespace/model}')
  